import * as account from './account';
import * as identity from './identity';
import * as text from './text';

// ============================================================
// Exports
export default {
    account,
    identity,
    text,
};
